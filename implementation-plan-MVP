MVP Status Snapshot (2025-08-31)

What’s Done
- Admin-gated Broker API:
  - Tenants list/get/create/revoke; admin bearer enforced when required
  - Chat proxy with optional KV sliding-window limits and per-tenant overrides
  - Usage/limits introspection: /v1/tenants/{id}/usage, /v1/tenants/{id}/limits, /v1/me, /v1/me/usage
  - Admin debug counters: /v1/debug/counters with filters/order/pagination; CLI parity
  - Idempotency middleware for /v1/chat (409 replay + X-Idempotency-Accepted)
- Data layer + CLI:
  - JSON tenant store (default) and optional SQL-backed store
  - SQL counter model + migration; KV→SQL compaction command
  - CLI: counters:show, data:compact-counters, broker:tenants:list, broker:limits:get|set, venice:* helpers, idem:purge
- Rate limiting + observability:
  - KV-backed sliding-window limiter (Redis optional) with standard headers
  - /metrics via starlette_exporter or builtin fallback
- Graph + tokenomics:
  - DIEM controller premium logic and standardized debug spans (debug_premium_span)
- Packaging + DevEx:
  - uv/pyproject.toml; .replit prefers uv with pip fallback; replit.nix preinstalls uv when available
  - Docker Compose for Postgres/Redis; limiter probe script; docs updated for uv + compose
- Tests (all passing locally):
  - Admin counters endpoint; admin limits endpoints; broker limiter (KV-only + Redis-skipped);
    idempotency purge CLI; DIEM thresholds

Remaining for MVP
- Replit Deployments validation
  - Runbook added in infra/replit/README.md (Deployments Validation). Remaining: execute probe on deployed Repl and record observed RPS vs. suggested defaults.
- SQL store smoke on Postgres / Replit SQL
  - Steps documented in README (SQL backend quick smoke). Remaining: run with BROKER_STORE_BACKEND=sql against a Postgres (or Replit SQL) instance and exercise /v1/debug/counters end-to-end.
- Docs polish
  - Completed: canonicalize idempotency TTL env as IDEMPOTENCY_TTL_SECONDS (alias IDEM_TTL_SECONDS still supported). Updated README and infra/replit docs.
- Release checklist
  - Completed: see MVP-RELEASE.md for tagging, env templates, and smoke-test runbooks (local/Replit).

References
- infra/replit/README.md: Deployments Validation, KV/Redis, Replit SQL setup
- README.md: Idempotency TTL, SQL backend quick smoke
- MVP-RELEASE.md: Release checklist and runbooks
